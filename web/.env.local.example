# Copy to .env.local and add your OpenRouter key (https://openrouter.ai/keys).
# The key stays server-side — it is read by the /api/extract route, never sent to the browser.
# (You can also paste a key in the UI per-run instead of setting this.)
OPENROUTER_API_KEY=
